#ifndef SIMPLE_
#define SIMPLE_

#include <stdio.h>
#include <SFML/Graphics.hpp>
#include <SFML/Window.hpp>
#include <sys/time.h>

const int MAX_ITERATION = 256;
const int MAX_R         = 100;

enum size_window
{
    LENGTH = 600,
    HIGH   = 600
};

enum first_point
{
    X0 = 400,
    Y0 = 300
};


#endif
